# 音声自動文字起こし・転送アプリ 仕様書

## 1. 概要

本アプリケーションは、マイク入力を常時監視し、発話を検知した場合に自動で録音・文字起こしを行い、その結果を外部APIへ送信する常駐型アプリケーションである。
主な用途として「受付・窓口での音声入力」を想定しています。

---

## 2. 全体フロー

1. アプリ起動
2. マイク入力待機状態に入る
3. マイクに対する発話を検知
4. 録音開始
5. 無音状態が2秒間継続
6. 録音停止
7. 音声データを文字起こし
8. 文字起こし結果を外部APIへ送信
9. 外部APIの処理完了を待機
10. 外部APIからwavファイルを受け取り、音声を再生
11. 完了後、再びマイク入力待機状態へ戻る

※ 上記処理を1サイクルとして無限ループで実行する

---

## 3. 機能要件

### 3.1 マイク入力監視
- アプリ起動後は常にマイク入力を監視する
- 音量レベル（RMS）を用いて発話を検知する
- 発話検知までは録音を行わない

### 3.2 録音制御
- 発話検知後に録音を開始する
- 録音中は一定間隔（例：100ms）で音声データを取得する
- 無音状態が2秒間連続した場合に録音を停止する

### 3.3 文字起こし
- 録音された音声データを音声認識エンジンで文字起こしする
- 日本語の音声認識に対応する
- 文字起こし結果が空の場合は送信処理を行わず待機状態へ戻る

### 3.4 外部API連携
- 文字起こし結果をJSON形式で外部APIへ送信する
- 外部APIのレスポンスを受信するまで次の録音処理を開始しない
- 通信失敗時はエラーを記録し、待機状態へ戻る

### 3.5 待機制御
- 外部APIの処理完了後、必ずマイク入力待機状態へ戻る
- 同時に複数の録音・送信処理は行わない

---

## 4. 非機能要件

### 4.1 実行形態
- 仮想環境を構築して実行
- 常駐型アプリケーション（デーモン）として動作
- systemd による起動・停止・自動再起動を想定

### 4.2 安定性
- 音声認識モデルは起動時に1回のみロードする
- 外部API通信失敗時でもアプリは停止しない

### 4.3 性能
- 発話検知はリアルタイムに近い応答性を持つ
- CPUのみの環境でも動作可能とする

---

## 5. 設定項目（想定）

| 項目 | 内容 | 例 |
|---|---|---|
| SAMPLE_RATE | サンプリングレート | 16000 |
| START_THRESHOLD | 発話検知音量 | 0.02 |
| SILENCE_THRESHOLD | 無音判定音量 | 0.01 |
| SILENCE_DURATION | 無音継続秒数 | 2.0 |
| SEND_API_URL | 送信先API URL | http://localhost:8080/chat |

---

## 6. 外部API仕様（例）

### リクエスト

```
curl -s -X POST http://localhost:8080/tts \                                                                                                                                                                  
    -H "Content-Type: application/json" \                                                                                                                                                                      
    -d '{"text":"こんにちは","speaker_id":0}'
```

### レスポンス

```
`200 OK`
- Content-Type: `audio/wav`
- Body: WAV バイナリ (PCM 16bit)
- Header `X-Response-Text`: URLエンコード済み応答テキスト
```

---

